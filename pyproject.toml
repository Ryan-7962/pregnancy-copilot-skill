[project]
name = "pregnancy-copilot-skill"
version = "0.1.8"
description = "Message-first, memory-first pregnancy Q&A and family memory agent skill."
requires-python = ">=3.10"
dependencies = ["pyyaml>=6.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
